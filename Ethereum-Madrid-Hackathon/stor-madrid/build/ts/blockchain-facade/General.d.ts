import { BlockchainProperties } from './BlockchainProperties';
export declare namespace General {
    function createCertificateForAssetOwner(blockchainProperties: BlockchainProperties, wh: number, assetId: number): Promise<any>;
}
